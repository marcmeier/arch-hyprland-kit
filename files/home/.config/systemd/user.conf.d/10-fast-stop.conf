# On logout, don't wait up to 90 s for apps that hang while stopping (e.g. VS Code's scope).
# A logout->login within that window made the next login fail (pam_systemd: UnitAllocationFailed).
[Manager]
DefaultTimeoutStopSec=10s
