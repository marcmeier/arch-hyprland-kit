# driftless: systemd based initramfs with the boot splash. sd-encrypt does nothing without an
# encrypted disk, so one line fits both. GPU modules for early KMS: 10-gpu.conf (install/bootstrap.sh).
HOOKS=(base systemd plymouth autodetect microcode modconf kms keyboard sd-vconsole block sd-encrypt filesystems fsck)
